import { DemoContainer } from "@/containers/demo/DemoContainer";

export const metadata = { title: "성향 테스트 체험 | 좌우지간", robots: "noindex" };

export default function DemoPage() {
  return <DemoContainer />;
}
